[pytest]
# Limit test discovery to our tests/ folder to avoid collecting vendor dataset tests
# that require heavy ML dependencies (torch, mmcv, etc.).
testpaths = tests
norecursedirs = datasets .venv venv build dist __pycache__

